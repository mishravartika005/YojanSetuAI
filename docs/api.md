# API

The Express API is mounted under `/api`. Route modules are intentionally empty during project setup. Authentication, profile, scheme, AI, and application contracts will be added with their validation and tests in subsequent phases.
